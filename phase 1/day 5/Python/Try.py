def  card_validetor(numbers):
	numbers =[]
numbers = list(input("Enter your card number"))
array_length = len(numbers)
String type = ""
if(card.charAt(0) == '4'):
     type = "Valid card"
	
if(card.charAt(0) == '5'):
	type = "valid Card"
	
if(card.charAt(0) == '3' or card.charAt(1) == '7'):
	type = "valid  Card"
	
if(card.charAt(0) == '6'):
	type = "valid  card"
	
elif  "invalid card":
      return type