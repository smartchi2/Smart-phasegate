
def  card_validetor(numbers):
	numbers = []
numbers = list(input("Enter your card number for  validation: "))
numbers = [item.replace('-', ' ') for item in numbers]
array_length = len(numbers)
if array_length == 16:
	print("You have entered a valid card please continue the validation")
			
if numbers.charAt(0) == '4' or numbers.charAt(0) == '5' or numbers.charAt(0) == '6':
		print("Card is valid")
			
else:
	print("invalid card entered")
	
if array_length % 4 == 0:
	print("Card numbers are valid")
elif:
	print("Entered card is invalid")
result = 0;
	for index in range(len(numbers)):
numbers = integer.parseInt(String.valueof(Numbers.charAt(index)))*2;
result += numbers

	
